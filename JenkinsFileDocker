pipeline {
    agent any
    environment {
        DOCKER_IMAGE = 'docker:stable'
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build and Run Docker') {
            steps {
                script {
                    def workspaceUnixPath = bat(script: 'echo %WORKSPACE% | sed "s|\\\\|/|g" | sed "s|C:|/c|"', returnStdout: true).trim()
                    echo "Workspace Unix Path: ${workspaceUnixPath}"
                    
                    docker.image(DOCKER_IMAGE).inside("-v /var/run/docker.sock:/var/run/docker.sock -w ${workspaceUnixPath}") {
                        sh 'docker version'
                        sh 'docker build -t my-app .'
                        sh 'docker run my-app'
                    }
                }
            }
        }
    }
}
